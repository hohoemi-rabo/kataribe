# 06 — 「今回だけ範囲指定」

## 目的

プリセットに保存せず、その場でドラッグ選択した範囲を1回だけ読み取る機能。イレギュラーなレイアウトの画面用。

- 参照: REQUIREMENTS.md §3.3（今回だけ範囲指定）、DESIGN.md §5（範囲選択オーバーレイ）
- 依存: 04, 05
- ⚠️ **ブラウザ実機での動作確認必須**

## Todo

- [x] メイン画面に button-secondary「今回だけ範囲指定」を配置（キャプチャ中のみ有効）
- [x] 押下 → その瞬間の静止画を表示 → チケット04の矩形選択オーバーレイを再利用してドラッグ選択
- [x] 確定ボタン（矩形直下の button-primary）→ そのまま読み取り実行（チケット05のフローに合流）
- [x] `presets` には保存しない。セクションの `preset_name` は `"manual"` で保存
- [x] 読み取り中フィードバック・テキスト先出し・エラーハンドリングはプリセット読み取りと同一挙動（読み取りコアを `runRead` として共通化）

### 動作確認（実機）

- [x] その場選択 → 読み取り → セクション保存まで一連で動く
- [x] プリセット一覧に増えていないこと
- [x] セクション一覧（チケット08以降）で preset_name が "manual" と表示されること（DB に "manual" で保存済みであることを SQL で確認。一覧画面での表示はチケット08で最終確認）

## メモ

- 矩形選択オーバーレイはチケット04のコンポーネントを props 分岐（保存モード / 即読み取りモード）で再利用し、重複実装しない
