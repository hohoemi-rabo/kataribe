# REQUIREMENTS.md — KATARIBE（カタリベ）

ゲーム内テキスト読み上げ・じぶんのプレイのあらすじ生成サービス

- 正式名称: **KATARIBE**（表記はローマ字大文字を主とする。読み: カタリベ）
- 由来: 語り部 — 記録を蓄え、声で物語を語り継ぐ人。本サービスのコンセプト「自分の進行度だけを知っている、自分専用の語り手」に対応

---

## 0. このドキュメントについて

- Claude Code への実装引き渡し用の要件定義書
- 実装は **1チケットずつ、承認を挟んで** 進めること（既存プロジェクトと同じ流儀）
- 実装順はチケット番号順（1 → 2 → 3 → …）

---

## 1. 企画概要

### 1.1 目的

開発者本人（ユーザーは1人のみ）が、オープンワールド / RPG をプレイする際に、
ゲーム内のジャーナル・コーデックス・ロア等の長文テキストを **AIに音声で読んでもらう** ためのwebサービス。

読ませたテキストはセクションとして蓄積し、任意のタイミングで
**「ここまでのあらすじ」を、蓄積したセクションのみを材料に** AIが生成・朗読する。

### 1.2 コンセプト

「自分の進行度だけを知っている、自分専用の解説者」

- 文章を読むのが苦手でも、ゲームの世界観・ストーリーを深く味わえるようにする
- 一般の考察動画と違い、**自分が読ませた範囲までしか知らない** のでネタバレが起きない
- エンディング後には全セクションから「俺のプレイの物語」をまとめてもらえる

### 1.3 利用シーン（確定済みの運用イメージ）

- プレイ環境: ゲーミングPC + **セカンドモニター**（ゲーム=メイン、ブラウザ=サブ）
- ゲームは **ボーダーレスウィンドウモード** で起動
- ゲームPCに開発環境・アプリのインストールは一切しない。**ブラウザでURLを開くだけ**

通常フロー（2回目以降）:

1. ゲーム内でジャーナル等を開く
2. セカンドモニターのブラウザで該当プリセットの「読んで」ボタンを押す
3. 数秒後、音声が流れてくる（その間ゲーム画面を眺めていればよい）

---

## 2. 技術スタック

| 層 | 技術 | 備考 |
|---|---|---|
| フロント | Next.js 15.5.22 (App Router) + TypeScript | バージョン固定 |
| CSS | Tailwind CSS **v3.4.17** | v4は使わない。手動でv3を導入すること |
| ホスティング | Vercel | |
| 認証・DB | Supabase（Google OAuth） | |
| AIプロキシ | Cloudflare Workers | Gemini API キーはWorker側で保持 |
| AI（画像→テキスト） | Gemini（Vision対応の安定版モデル） | 実装時に最新の安定版モデルIDを確認 |
| AI（あらすじ生成） | Gemini（同上） | |
| AI（音声 / TTS） | Gemini TTS | ⚠️ preview系モデルのため **実装時に最新のモデルIDを必ず確認**。失敗時は Web Speech API にフォールバック |
| 画面キャプチャ | ブラウザ標準 `getDisplayMedia` | インストール不要 |

### 2.1 認証・アクセス制限

- Google OAuth（Supabase Auth）
- **利用者は開発者本人のみ**。許可メールアドレスのallowlist（環境変数）に一致しないアカウントはログイン後に弾く
- RLS: 全テーブル `user_id = auth.uid()` で制限

---

## 3. 機能要件

### 3.1 画面キャプチャ（コア機能）

- 「キャプチャ開始」ボタンで `getDisplayMedia` を呼び、ユーザーがゲームウィンドウを選択
- 以降、video要素にストリームを保持し、ボタン押下時に **その瞬間の1フレーム** をcanvasに取得する
- キャプチャ中であることが分かる状態表示（小さなライブプレビュー + 「キャプチャ中」バッジ）
- タブ再読み込み・ストリーム切断時は「キャプチャ開始」に戻る

### 3.2 範囲プリセット（ゲームごと・初回のみ登録）

- ゲームごとに **複数の範囲プリセット** を登録できる（例: 「ジャーナル」「コーデックス」「アイテム説明」）
- 登録フロー:
  1. キャプチャ中に「範囲を登録」を押す → その瞬間の静止画を画面に表示
  2. マウスドラッグで矩形選択（スクショに矩形を描くのと同じ操作感）
  3. プリセット名を入力して保存
- プリセットは **元画像に対する相対座標（0〜1の比率）** で保存する
  （ゲーム解像度やキャプチャ解像度が変わってもズレないようにするため）
- プリセットの編集（範囲の再登録・名前変更）・削除が可能

### 3.3 読み取り実行（「読んで」ボタン）

- プリセットごとにボタンとして並ぶ。押すと:
  1. 現在フレームを取得し、プリセット範囲で切り抜き
  2. 切り抜き画像をWorker経由でGeminiに送信し、テキスト化
  3. 結果を **セクション** としてDBに保存（後述）
  4. 保存したテキストを **即座に音声で再生**（3.5参照）
- テキスト化のプロンプト（固定・Worker側で付与）:
  - 「この画像内の **本文テキストのみ** を忠実に書き起こす」
  - 「UI要素・ボタンラベル・メニュー項目・スクロールバー等は無視する」
  - 「日本語のテキストは日本語のまま書き起こす。要約・意訳をしない」
- **「今回だけ範囲指定」ボタン** も用意する:
  - 押すとその瞬間の静止画を表示 → その場でドラッグ選択 → 読み取り実行
  - プリセットには保存しない（イレギュラーなレイアウトの画面用）

### 3.4 セクション管理

- セクションはゲームごとに **自動連番**（セクション1, 2, 3…）で時系列に蓄積
- セクション一覧画面:
  - ゲームを選択 → セクションを新しい順 / 古い順で一覧
  - 各セクション: 連番、作成日時、プリセット名、本文テキスト、切り抜き画像サムネイル
  - 個別に「もう一度読み上げ」「テキスト修正」「削除」が可能
  - テキスト修正: AIの誤認識（固有名詞など）を手で直せるようにする。あらすじ生成の材料になるため重要

### 3.5 音声再生（TTS）

- 読み上げ対象: ①セクションのテキスト化直後の自動再生 ②セクションの再読み上げ ③あらすじの読み上げ
- Gemini TTS（Worker経由）で音声を生成し、ブラウザで再生
- 音声の指定: 落ち着いた日本語の朗読向けボイス（実装時に候補から選定。設定画面で変更できると望ましいが、MVPでは固定でも可）
- 再生コントロール: 再生 / 一時停止 / 停止。長文でも途中で止められること
- **フォールバック**: Gemini TTSが失敗した場合は Web Speech API（ja-JP voice）で読み上げる
  （習い事ノートアプリで実装済みのパターンを踏襲: Gemini TTS → 失敗時 speechSynthesis）
- 生成した音声はセクションに紐づけて保存はせず、都度生成でよい（MVP方針。コスト・容量を優先）

### 3.6 あらすじ生成（本命機能）

- 「ここまでのあらすじ」ボタンで、選択中のゲームの **全セクション（または範囲指定: セクションN〜M）** を材料にAIが物語風のまとめを生成
- 生成タイミングは自由（途中経過でも、エンディング後の総まとめでも）
- **ネタバレ防止の絶対制約**（プロンプトに固定で入れる。Worker側で付与）:
  - 「提供されたセクションのテキスト **のみ** を情報源とすること」
  - 「このゲームに関する外部知識・事前知識を **一切使用しない** こと。セクションに書かれていない人物・地名・出来事・今後の展開に言及してはならない」
  - 「推測で補完せず、書かれている範囲で構成すること」
- 生成結果は保存し、履歴として一覧できる（生成日時、対象セクション範囲、本文）
- 生成したあらすじも音声で読み上げ可能
- 出力スタイル: 「だ・である」ではなく、語り聞かせるような自然な口調（朗読前提のため）

### 3.7 ゲーム管理

- ゲームの登録（タイトル名のみ。マスターデータなし・自由入力）
- ゲームの切り替え（ヘッダー等で常に選択中のゲームが分かること）
- ゲームの削除（配下のプリセット・セクション・あらすじも削除。確認ダイアログ必須）

---

## 4. 非機能要件・制約

- **レスポンス速度**: 「読んで」ボタン押下 → 音声再生開始まで、体感で待てる長さにする。テキスト化完了時点でまず画面にテキストを表示し、TTS生成を待たせない工夫（テキスト先出し）をすること
- **コスト管理**: Worker側でレート制限（例: 1日あたりの呼び出し上限。占いキッカケ等と同様のJSTリセット方式）。個人利用のため緩めでよいが、暴走保険として必須
- **画像の保存**: セクションの切り抜き画像はSupabase Storageに保存（サムネイル用途）。元のフル画面キャプチャは保存しない
- **対象ブラウザ**: PC版Chrome最新のみ。スマホ対応は **しない**（getDisplayMedia前提のため）
- **多ユーザー対応・公開**: しない。個人利用のみ

---

## 5. データベース設計（Supabase）

```
games
- id (uuid, PK)
- user_id (uuid)
- title (text)            -- 自由入力
- created_at (timestamptz)

presets
- id (uuid, PK)
- user_id (uuid)
- game_id (uuid, FK → games)
- name (text)             -- 例: ジャーナル / コーデックス
- x, y, w, h (float)      -- 相対座標（0〜1）
- created_at (timestamptz)

sections
- id (uuid, PK)
- user_id (uuid)
- game_id (uuid, FK → games)
- seq (int)               -- ゲーム内自動連番
- preset_name (text)      -- 使用したプリセット名（今回だけ範囲指定は "manual"）
- content (text)          -- 書き起こしテキスト（編集可能）
- image_path (text)       -- Storage上の切り抜き画像
- created_at (timestamptz)

summaries
- id (uuid, PK)
- user_id (uuid)
- game_id (uuid, FK → games)
- from_seq (int)          -- 対象セクション範囲
- to_seq (int)
- content (text)          -- 生成されたあらすじ
- created_at (timestamptz)
```

- 全テーブルにRLS（`user_id = auth.uid()`）
- `sections.seq` はゲームごとに `max(seq) + 1` で採番（同時書き込みは想定しない）

---

## 6. Cloudflare Worker API

| エンドポイント | 役割 |
|---|---|
| `POST /transcribe` | 切り抜き画像（base64）→ 本文テキスト（Gemini Vision） |
| `POST /summarize` | セクションテキスト配列 → あらすじ（Gemini、ネタバレ防止プロンプト固定） |
| `POST /tts` | テキスト → 音声（Gemini TTS、PCM→WAV変換して返す） |

- 認証: Supabase JWTの検証（既存プロジェクトのWorkerパターンを踏襲）
- Gemini APIキーはWorkerの環境変数のみに保持。フロントには一切出さない
- レート制限: Worker側で実装（JST日次リセット）

---

## 7. 画面構成

| 画面 | 内容 |
|---|---|
| ログイン | Googleログインのみ。allowlist外は拒否 |
| メイン（キャプチャ画面） | キャプチャ開始/停止、ライブプレビュー、プリセットボタン群、「今回だけ範囲指定」、直近の読み取り結果テキスト、再生コントロール |
| 範囲登録モーダル | 静止画表示 + ドラッグ矩形選択 + プリセット名入力 |
| セクション一覧 | ゲーム別、時系列、再読み上げ / 編集 / 削除 |
| あらすじ | 生成ボタン（範囲指定つき）、履歴一覧、読み上げ |
| 設定 | ゲーム管理（追加 / 削除）、プリセット管理 |

- デザイン方針: セカンドモニターに置きっぱなしにする画面なので、**ボタンが大きく・暗め基調**（ゲーム中に眩しくない）。詳細はDESIGN.mdで別途定義（本ドキュメントの対象外）

---

## 8. 実装チケット

| # | チケット | 内容 |
|---|---|---|
| 1 | プロジェクト基盤 | Next.js 15.5.22 + Tailwind v3.4.17 + Supabase接続 + Google OAuth + allowlist |
| 2 | DB・ゲーム管理 | 5章のテーブル作成 + RLS + ゲーム追加/切替/削除 |
| 3 | 画面キャプチャ | getDisplayMedia + ライブプレビュー + フレーム取得 |
| 4 | 範囲プリセット | 静止画表示 + ドラッグ矩形選択 + 相対座標保存 + 編集/削除 |
| 5 | Worker: transcribe | 画像→テキスト化API + フロント接続 + セクション保存（連番） |
| 6 | 「今回だけ範囲指定」 | その場ドラッグ→読み取り（プリセット保存なし） |
| 7 | Worker: tts + 再生 | Gemini TTS + PCM→WAV + フォールバック + 再生コントロール |
| 8 | セクション一覧 | 一覧 / 再読み上げ / テキスト編集 / 削除 |
| 9 | Worker: summarize | あらすじ生成（ネタバレ防止プロンプト） + 保存 + 履歴 + 読み上げ |
| 10 | 仕上げ | レート制限、エラーハンドリング、状態表示の磨き込み |

- 各チケット完了ごとに動作確認 → 承認 → 次へ
- チケット3・4はブラウザ実機での動作確認が必須（getDisplayMediaは自動テスト不可のため）

---

## 9. スコープ外（やらないこと）

- スマホ / タブレット対応
- 多ユーザー化・一般公開
- ゲーム画面の常時監視・自動読み取り（ともにのような自動実況はしない。読むタイミングは必ず本人がボタンで指定する）
- 音声ファイルの永続保存
- ゲームタイトルのマスターデータ・API連携
- 外部知識を使った補足解説（ネタバレ防止のため明示的にスコープ外）

---

## 10. 未確定事項（実装時に確認）

- Gemini各モデルの最新の安定版モデルID（特にTTSはpreview系のため必ず確認）
- TTSボイスの選定（朗読向けの落ち着いた日本語ボイス）
- レート制限の具体値（初期値: transcribe 50回/日、summarize 10回/日、tts 60回/日 程度から調整）